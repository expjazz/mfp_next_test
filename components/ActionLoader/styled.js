import styled from '@emotion/styled';

const ActionLoaderStyled = styled.div`
  position: fixed;
  display: flex;
  left: 0;
  right: 0;
  bottom: 0;
  top: 0;
  z-index: 20;
  background-color: rgba(0, 0, 0, .5);
`;

export default ActionLoaderStyled;
